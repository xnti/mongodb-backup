import type { Options } from './Options';
/**
 * Build an executable
 * @param {Options} options
 * @returns An empty promise which is resolved when the executable is built
 */
declare function exe(options: Options): Promise<void>;
export = exe;
